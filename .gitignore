# --- Secrets: never commit real Wi-Fi credentials ---
firmware/main/wifi_secrets.h

# --- ESP-IDF build output (regenerated by `idf.py build`) ---
build/
firmware/build/
firmware/sdkconfig.old
managed_components/
dependencies.lock

# --- Python ---
__pycache__/
*.pyc
.venv/
venv/

# --- Excluded legacy dataset (pre-fix schema, mixed subcarrier counts,
#     known-contaminated - see docs/PROJECT_HISTORY.md section 1) ---
csi_dataset_20260723_184612/

# --- Regenerable analysis output ---
model_analysis.json
model_evaluation_report.pdf
report_output/

# --- Local-only model backups (not meant for the shared repo) ---
csi_model_backup_*.joblib

# --- Editor/OS cruft ---
.vscode/
.idea/
Thumbs.db
.DS_Store